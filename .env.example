VITE_API_KEY=your_google_gemini_api_key_here
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=your_supabase_anon_or_public_key

# Security notes:
# - Do NOT commit real secrets to version control.
# - For sensitive server-side operations (billing/privileged keys) host an API proxy or use serverless functions.
# - These VITE_ variables will be embedded into the frontend bundle at build time; treat them as public.
